global:
  scrape_interval: 30s
rule_files:
  - alerts.yml
scrape_configs:
  - job_name: centinell-forensics
    metrics_path: /metrics
    authorization:
      credentials_file: /run/secrets/centinell_metrics_token
    static_configs:
      - targets: ["app:3000"]
